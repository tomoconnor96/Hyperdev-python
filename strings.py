# main objective - to strip the string in variable hero so any char before and after the string is stripped
Hero = "$$$Superman$$$"#example varaible and string given for task
print(Hero.strip('$')) # print varaible hero so the char $ is stripped from the str - prints "Superman"
